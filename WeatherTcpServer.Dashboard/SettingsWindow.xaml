<Window x:Class="WeatherTcpServer.Dashboard.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Height="400" Width="500" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource AppBgBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="*" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="⚙️ Settings" FontSize="24" FontWeight="Bold" />
            <TextBlock Text="Configure server and appearance" Foreground="{StaticResource MutedTextBrush}" FontSize="13" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Settings Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Server Configuration -->
                <Border Background="{StaticResource PanelBrush}" CornerRadius="12" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="🌐" FontSize="18" Margin="0,0,8,0" />
                            <TextBlock Text="TCP Server Configuration" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="16" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="16" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="IP Address" Foreground="{StaticResource MutedTextBrush}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                                <TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}" Padding="12,10" />
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="2">
                                <TextBlock Text="Port" Foreground="{StaticResource MutedTextBrush}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6" />
                                <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Padding="12,10" />
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal">
                                <Button Content="▶ Start Server" Width="120" Padding="12,10" Command="{Binding StartCommand}" Margin="0,0,12,0" />
                                <Button Content="⏹ Stop Server" Width="120" Padding="12,10" Style="{StaticResource DangerButton}" Command="{Binding StopCommand}" />
                            </StackPanel>

                            <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Text="💡 Tip: Send 1 JSON per line (newline-delimited)" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Appearance Settings -->
                <Border Background="{StaticResource PanelBrush}" CornerRadius="12" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="🎨" FontSize="18" Margin="0,0,8,0" />
                            <TextBlock Text="Appearance" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" />
                        </StackPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Theme" FontWeight="SemiBold" FontSize="14" />
                                <TextBlock Text="Switch between light and dark mode" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,4,0,0" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="☀️" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                                <ToggleButton Width="50" Height="28" Padding="3" IsChecked="{Binding IsDarkMode}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{StaticResource BorderBrush}" CornerRadius="14" Padding="3">
                                                <Grid>
                                                    <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="{StaticResource PanelBrush}" HorizontalAlignment="Left" />
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <TextBlock Text="🌙" FontSize="16" VerticalAlignment="Center" Margin="8,0,0,0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="80" Padding="12,8" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>